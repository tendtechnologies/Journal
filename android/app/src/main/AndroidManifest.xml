<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!--
      Required so the app can see the Health Connect provider on Android 13
      and below, where Health Connect ships as a separate app. Without this,
      HealthConnectClient.getSdkStatus() cannot detect it.
    -->
    <queries>
        <package android:name="com.google.android.apps.healthdata" />
    </queries>

    <!-- Sign-in and sync. Writing works entirely offline. -->
    <uses-permission android:name="android.permission.INTERNET" />

    <!--
      Steps and sleep are the only health data this app reads, and both are
      optional: every screen works with them absent. Nothing is ever written
      back to Health Connect.
    -->
    <uses-permission android:name="android.permission.health.READ_STEPS" />
    <uses-permission android:name="android.permission.health.READ_SLEEP" />

    <!--
      Without this, Health Connect returns only the last 30 days of data written
      by OTHER apps and silently ignores the rest of the requested range — so
      every correlation longer than a month would be computed from a truncated
      series without saying so.
    -->
    <uses-permission android:name="android.permission.health.READ_HEALTH_DATA_HISTORY" />

    <!--
      Coarse only, and never background. Location is read exactly once, when the
      writer taps "use my location" on an entry — there is no tracking, and the
      app asks for the permission at that moment rather than on first launch.
    -->
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/Theme.Journal"
        android:supportsRtl="true">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustResize"
            android:theme="@style/Theme.Journal">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Health Connect opens this to show why the app wants the data (Android 13 and below). -->
        <activity
            android:name=".PermissionsRationaleActivity"
            android:exported="true"
            android:theme="@style/Theme.Journal">
            <intent-filter>
                <action android:name="androidx.health.ACTION_SHOW_PERMISSIONS_RATIONALE" />
            </intent-filter>
        </activity>

        <!-- Same thing on Android 14+, where Health Connect is part of the platform. -->
        <activity-alias
            android:name="ViewPermissionUsageActivity"
            android:exported="true"
            android:targetActivity=".PermissionsRationaleActivity"
            android:permission="android.permission.START_VIEW_PERMISSION_USAGE">
            <intent-filter>
                <action android:name="android.intent.action.VIEW_PERMISSION_USAGE" />
                <category android:name="android.intent.category.HEALTH_PERMISSIONS" />
            </intent-filter>
        </activity-alias>

    </application>
</manifest>
